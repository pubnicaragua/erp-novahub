import { useEffect, useState } from 'react';
import { useLocation } from 'react-router';
import { ChevronDown, FileText, LockKeyhole, Printer, ShieldCheck, WalletCards } from 'lucide-react';

const API = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';
async function publicRequest(path: string, init: RequestInit = {}) { const response = await fetch(`${API}${path}`, { ...init, headers: { 'Content-Type': 'application/json', ...(init.headers || {}) } }); const body = await response.json().catch(() => ({})); if (!response.ok) { const raw = body?.message; const message = typeof raw === 'string' ? raw : raw?.message || raw?.error || body?.error || 'No se pudo abrir el enlace.'; throw new Error(message); } return body; }

const documentTypeLabels: Record<string, string> = { estimate: 'Cotización', 'sales-order': 'Orden de venta', invoice: 'Factura', 'recurring-invoice': 'Factura recurrente', 'payment-received': 'Pago recibido', 'sales-return': 'Devolución', 'credit-note': 'Nota de crédito' };
const statusLabels: Record<string, string> = { DRAFT: 'Borrador', PENDING: 'Pendiente', SENT: 'Enviada', APPROVED: 'Aprobada', PAID: 'Pagada', PARTIAL: 'Pago parcial', OVERDUE: 'Vencida', CANCELLED: 'Cancelada', COMPLETED: 'Completada', ACTIVE: 'Activa', INACTIVE: 'Inactiva' };
const paymentMethodLabels: Record<string, string> = { CASH: 'Efectivo', CARD: 'Tarjeta', TRANSFER: 'Transferencia bancaria', CHECK: 'Cheque', CREDIT: 'Crédito', OTHER: 'Otro' };
const labelFor = (value: unknown, labels: Record<string, string>, fallback = '—') => labels[String(value || '').toUpperCase()] || (value ? String(value) : fallback);
const formatDate = (value: unknown) => value ? new Intl.DateTimeFormat('es-NI', { dateStyle: 'medium' }).format(new Date(String(value))) : '—';
const formatAmount = (value: unknown, currency = 'NIO') => `${new Intl.NumberFormat('es-NI', { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(Number(value || 0))} ${currency}`;

function AccessShell({ children, company }: { children: React.ReactNode; company?: any }) { return <main className="min-h-screen bg-slate-950 px-4 py-8 text-slate-100"><div className="mx-auto max-w-5xl"><header className="mb-6 flex items-center gap-3 rounded-2xl border border-slate-800 bg-slate-900/90 p-5"><div className="flex size-11 items-center justify-center rounded-xl bg-emerald-500/15 text-emerald-400"><ShieldCheck className="size-6" /></div><div><p className="text-lg font-semibold">{company?.name || 'Acceso seguro'}</p><p className="text-sm text-slate-400">Portal público de documentos</p></div></header>{children}<p className="mt-6 text-center text-xs text-slate-500">Este enlace es temporal y está protegido por NovaHub.</p></div></main>; }

function OtpForm({ token, mode, maskedTarget, onVerified }: { token: string; mode: 'document' | 'portal'; maskedTarget?: string | null; onVerified: (session: string) => void }) { const [challengeId, setChallengeId] = useState<string | null>(null); const [code, setCode] = useState(''); const [message, setMessage] = useState(''); const requestCode = async () => { try { const result = await publicRequest(`/public-access/${mode}/${token}/otp`, { method: 'POST' }); setChallengeId(result.challengeId); setMessage(result.devCode ? `Código de desarrollo: ${result.devCode}` : `Enviamos un código a ${result.maskedTarget}.`); } catch (error: any) { setMessage(error.message); } }; const verify = async () => { try { const result = await publicRequest(`/public-access/${mode}/${token}/verify`, { method: 'POST', body: JSON.stringify({ challengeId, code }) }); onVerified(result.sessionToken); } catch (error: any) { setMessage(error.message); } }; return <div className="mx-auto max-w-md rounded-2xl border border-slate-800 bg-slate-900 p-7"><LockKeyhole className="mb-4 size-8 text-emerald-400" /><h1 className="text-xl font-semibold">Verifica tu acceso</h1><p className="mt-2 text-sm text-slate-400">Por seguridad necesitamos confirmar tu identidad{maskedTarget ? ` en ${maskedTarget}` : ''}.</p>{message && <p className="mt-4 rounded-lg bg-slate-800 p-3 text-sm text-emerald-300">{message}</p>}{!challengeId ? <button onClick={requestCode} className="mt-6 w-full rounded-xl bg-emerald-500 px-4 py-3 font-semibold text-slate-950">Enviar código</button> : <div className="mt-6 space-y-3"><input value={code} onChange={e => setCode(e.target.value)} inputMode="numeric" maxLength={6} placeholder="Código de 6 dígitos" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-emerald-400" /><button onClick={verify} className="w-full rounded-xl bg-emerald-500 px-4 py-3 font-semibold text-slate-950">Continuar</button></div>}</div>; }

function DocumentView({ data, permissions }: { data: any; permissions: any }) { const { company, document, customer, items } = data; return <AccessShell company={company}><section className="rounded-2xl border border-slate-800 bg-white p-6 text-slate-900 shadow-2xl print:shadow-none"><div className="flex justify-between border-b border-slate-200 pb-5"><div><h1 className="text-2xl font-bold text-emerald-600">{company.name}</h1><p className="text-sm text-slate-500">{labelFor(data.type, documentTypeLabels, 'Documento')}</p></div><div className="text-right"><h2 className="text-xl font-bold uppercase">{document.number}</h2><p className="text-sm text-slate-500">Fecha: {formatDate(document.date)}</p><p className="text-sm text-slate-500">Estado: {labelFor(document.status, statusLabels, 'Emitido')}</p></div></div><div className="grid gap-4 py-5 sm:grid-cols-2"><div><p className="text-xs font-bold uppercase text-emerald-600">Datos del cliente</p><p className="font-semibold">{customer.name}</p><p className="text-sm text-slate-500">{customer.razonSocial || ''}</p><p className="text-sm text-slate-500">{customer.email || customer.phone || ''}</p><p className="text-sm text-slate-500">{customer.address || ''}{customer.city ? ` · ${customer.city}` : ''}</p></div><div className="text-left sm:text-right"><p className="text-xs font-bold uppercase text-slate-500">Información comercial</p><p className="text-sm">Código: {customer.code || '—'}</p><p className="text-sm">Identificación fiscal: {customer.ruc || customer.taxId || '—'}</p></div></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="bg-emerald-500 text-white"><th className="p-3 text-left">Descripción</th><th className="p-3 text-right">Cantidad</th><th className="p-3 text-right">Precio unitario</th><th className="p-3 text-right">Total</th></tr></thead><tbody>{items.length ? items.map((item: any, index: number) => <tr key={index} className="border-b border-slate-200"><td className="p-3">{item.description}</td><td className="p-3 text-right">{item.quantity}</td><td className="p-3 text-right">{formatAmount(item.unitPrice, document.currency)}</td><td className="p-3 text-right">{formatAmount(item.total, document.currency)}</td></tr>) : <tr><td colSpan={4} className="p-5 text-center text-slate-500">Sin líneas de detalle</td></tr>}</tbody></table></div><div className="mt-6 ml-auto max-w-xs space-y-2 text-right"><p>Subtotal: {formatAmount(document.subtotal, document.currency)}</p><p>Impuestos: {formatAmount(document.taxAmount, document.currency)}</p><p className="border-t border-slate-200 pt-2 text-lg font-bold text-emerald-600">Total: {formatAmount(document.total, document.currency)}</p></div><div className="mt-8 flex gap-3 print:hidden">{permissions.print && <button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-xl border border-slate-300 px-4 py-2 font-semibold"><Printer className="size-4" /> Imprimir</button>}{permissions.download && <button onClick={() => window.print()} className="rounded-xl bg-emerald-600 px-4 py-2 font-semibold text-white">Descargar PDF</button>}</div></section></AccessShell>; }

function PortalView({ data }: { data: any }) {
  const customer = data.customer;
  const [selectedDocument, setSelectedDocument] = useState<any>(null);
  const selected = selectedDocument?.document;
  return <AccessShell company={data.company}><div className="grid gap-5 md:grid-cols-3"><div className="md:col-span-3 rounded-2xl border border-slate-800 bg-slate-900 p-6"><p className="text-sm text-slate-400">Información del cliente</p><h1 className="text-2xl font-semibold">{customer.name}</h1><div className="mt-4 grid gap-3 text-sm text-slate-300 sm:grid-cols-2 lg:grid-cols-3"><p><span className="text-slate-500">Código:</span> {customer.code || '—'}</p><p><span className="text-slate-500">Correo:</span> {customer.email || '—'}</p><p><span className="text-slate-500">Teléfono:</span> {customer.phone || '—'}</p><p><span className="text-slate-500">RUC / identificación:</span> {customer.ruc || customer.taxId || '—'}</p><p><span className="text-slate-500">Dirección:</span> {customer.address || '—'}</p><p><span className="text-slate-500">Ciudad:</span> {customer.city || '—'}</p><p><span className="text-slate-500">Departamento:</span> {customer.department || '—'}</p><p><span className="text-slate-500">País:</span> {customer.country || '—'}</p><p><span className="text-slate-500">Contacto:</span> {customer.contactName || '—'}</p></div></div>{[['Total facturado', data.summary.totalInvoiced], ['Total pagado', data.summary.totalPaid], ['Saldo pendiente', data.summary.balance]].map(([label, value]) => <div key={String(label)} className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><p className="text-sm text-slate-400">{label}</p><p className="mt-2 text-2xl font-bold text-emerald-400">{formatAmount(value)}</p></div>)}<div className="md:col-span-3 rounded-2xl border border-slate-800 bg-slate-900 p-6"><div className="mb-2 flex items-center gap-2"><WalletCards className="size-5 text-emerald-400" /><h2 className="text-lg font-semibold">Documentos del cliente</h2></div><p className="mb-4 text-sm text-slate-400">Selecciona una transacción para consultar todos sus detalles.</p><div className="space-y-2">{data.documents.length ? data.documents.map((doc: any) => { const isSelected = selectedDocument?.document.id === doc.document.id && selectedDocument?.type === doc.type; return <div key={`${doc.type}-${doc.document.id}`} role="button" tabIndex={0} onClick={() => setSelectedDocument(isSelected ? null : doc)} onKeyDown={event => { if (event.key === 'Enter' || event.key === ' ') setSelectedDocument(isSelected ? null : doc); }} className={`cursor-pointer rounded-xl border p-4 transition-colors ${isSelected ? 'border-emerald-400 bg-emerald-500/10' : 'border-slate-800 hover:border-emerald-500/60'}`}><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-semibold">{labelFor(doc.type, documentTypeLabels, 'Documento')} · {doc.document.number}</p><p className="text-sm text-slate-400">Fecha: {formatDate(doc.document.date)} · Estado: {labelFor(doc.document.status, statusLabels, 'Emitido')}</p></div><div className="flex items-center gap-3"><p className="font-semibold text-emerald-400">{formatAmount(doc.document.total, doc.document.currency)}</p><ChevronDown className={`size-4 text-slate-400 transition-transform ${isSelected ? 'rotate-180 text-emerald-400' : ''}`} /></div></div>{isSelected && <TransactionDetail documentData={doc} />}</div>; }) : <p className="rounded-xl border border-dashed border-slate-700 p-5 text-sm text-slate-400">No hay documentos registrados.</p>}</div></div></div></AccessShell>;
}

function TransactionDetail({ documentData }: { documentData: any }) { const { document, items = [], payments = [], customer } = documentData; return <div className="mt-5 space-y-5 border-t border-slate-700 pt-5" onClick={event => event.stopPropagation()}><div className="grid gap-3 text-sm text-slate-300 sm:grid-cols-2 lg:grid-cols-4"><p><span className="text-slate-500">Fecha:</span> {formatDate(document.date)}</p><p><span className="text-slate-500">Vencimiento:</span> {formatDate(document.dueDate)}</p><p><span className="text-slate-500">Moneda:</span> {document.currency || 'NIO'}</p><p><span className="text-slate-500">Cliente:</span> {customer.name}</p></div><div className="overflow-x-auto rounded-xl border border-slate-700"><table className="w-full text-sm"><thead className="bg-slate-800"><tr><th className="p-3 text-left">Producto o servicio</th><th className="p-3 text-right">Cantidad</th><th className="p-3 text-right">Precio unitario</th><th className="p-3 text-right">Total</th></tr></thead><tbody>{items.length ? items.map((item: any, index: number) => <tr key={index} className="border-t border-slate-800"><td className="p-3">{item.description || '—'}</td><td className="p-3 text-right">{item.quantity ?? '—'}</td><td className="p-3 text-right">{formatAmount(item.unitPrice, document.currency)}</td><td className="p-3 text-right">{formatAmount(item.total, document.currency)}</td></tr>) : <tr><td colSpan={4} className="p-4 text-center text-slate-400">Este documento no tiene líneas de detalle.</td></tr>}</tbody></table></div><div className="grid gap-4 lg:grid-cols-2"><div className="space-y-2 rounded-xl border border-slate-700 p-4 text-sm"><p className="font-semibold text-white">Resumen de la transacción</p><p>Subtotal: <span className="float-right">{formatAmount(document.subtotal, document.currency)}</span></p><p>Impuestos: <span className="float-right">{formatAmount(document.taxAmount, document.currency)}</span></p><p>Total: <span className="float-right font-bold text-emerald-400">{formatAmount(document.total, document.currency)}</span></p><p>Pagado: <span className="float-right">{formatAmount(document.amountPaid, document.currency)}</span></p><p>Saldo: <span className="float-right font-bold">{formatAmount(document.balance, document.currency)}</span></p>{document.paymentMethod && <p>Método de pago: <span className="float-right">{labelFor(document.paymentMethod, paymentMethodLabels, '—')}</span></p>}{document.reference && <p>Referencia: <span className="float-right">{document.reference}</span></p>}</div><div className="space-y-3 rounded-xl border border-slate-700 p-4 text-sm"><p className="font-semibold text-white">Pagos relacionados</p>{payments.length ? payments.map((payment: any, index: number) => <div key={index} className="flex justify-between border-t border-slate-800 pt-2"><span>{payment.number || 'Pago'} · {formatDate(payment.date)}</span><span className="font-semibold text-emerald-400">{formatAmount(payment.amount, payment.currency || document.currency)}</span></div>) : <p className="text-slate-400">No hay pagos relacionados.</p>}</div></div>{document.notes && <div className="rounded-xl border border-slate-700 bg-slate-800/50 p-4 text-sm"><p className="font-semibold text-white">Observaciones</p><p className="mt-1 text-slate-300">{document.notes}</p></div>}</div>; }

export function PublicAccessPage({ mode }: { mode: 'document' | 'portal' }) {
  const location = useLocation();
  const token = location.pathname.split('/').filter(Boolean).pop() || '';
  const storageKey = `novahub-public-session:${token}`;
  const [data, setData] = useState<any>(null);
  const [gate, setGate] = useState<any>(null);
  const [error, setError] = useState('');

  useEffect(() => {
    let cancelled = false;
    const load = async () => {
      try {
        if (mode === 'document') {
          const result = await publicRequest(`/public-access/document/${token}`);
          if (cancelled) return;
          if (result?.requiresVerification) setGate(result); else setData(result);
          return;
        }

        const savedSession = localStorage.getItem(storageKey);
        if (savedSession) {
          try {
            const result = await publicRequest('/public-access/portal/session', { headers: { 'x-public-session': savedSession } });
            if (!cancelled) setData(result);
            return;
          } catch {
            localStorage.removeItem(storageKey);
          }
        }

        const result = await publicRequest(`/public-access/portal/${token}`);
        if (cancelled) return;
        if (result?.requiresVerification) setGate(result);
        else {
          if (result?.sessionToken) localStorage.setItem(storageKey, result.sessionToken);
          setData(result);
        }
      } catch (e: any) {
        if (!cancelled) setError(e.message);
      }
    };
    if (token) load(); else setError('Enlace incompleto.');
    return () => { cancelled = true; };
  }, [mode, token, storageKey]);

  if (error) return <AccessShell><div className="rounded-2xl border border-rose-500/30 bg-slate-900 p-8 text-center"><FileText className="mx-auto mb-3 size-9 text-rose-400" /><h1 className="text-xl font-semibold">Enlace no disponible</h1><p className="mt-2 text-slate-400">{error}</p></div></AccessShell>;
  if (gate) return <AccessShell><OtpForm token={token} mode={mode} maskedTarget={gate.maskedTarget} onVerified={async session => { localStorage.setItem(storageKey, session); if (mode === 'document') { const result = await publicRequest(`/public-access/document/${token}`, { headers: { 'x-public-session': session } }); setData(result); setGate(null); } else { const result = await publicRequest('/public-access/portal/session', { headers: { 'x-public-session': session } }); setData(result); setGate(null); } }} /></AccessShell>;
  if (!data) return <AccessShell><div className="p-12 text-center text-slate-400">Cargando documento…</div></AccessShell>;
  return mode === 'document' ? <DocumentView data={data.data} permissions={data.permissions} /> : <PortalView data={data} />;
}
