import { useEffect, useState } from 'react';
import { useLocation } from 'react-router';
import { FileText, LockKeyhole, Printer, ShieldCheck, WalletCards } from 'lucide-react';

const API = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';
async function publicRequest(path: string, init: RequestInit = {}) { const response = await fetch(`${API}${path}`, { ...init, headers: { 'Content-Type': 'application/json', ...(init.headers || {}) } }); const body = await response.json().catch(() => ({})); if (!response.ok) { const raw = body?.message; const message = typeof raw === 'string' ? raw : raw?.message || raw?.error || body?.error || 'No se pudo abrir el enlace.'; throw new Error(message); } return body; }

function AccessShell({ children, company }: { children: React.ReactNode; company?: any }) { return <main className="min-h-screen bg-slate-950 px-4 py-8 text-slate-100"><div className="mx-auto max-w-5xl"><header className="mb-6 flex items-center gap-3 rounded-2xl border border-slate-800 bg-slate-900/90 p-5"><div className="flex size-11 items-center justify-center rounded-xl bg-emerald-500/15 text-emerald-400"><ShieldCheck className="size-6" /></div><div><p className="text-lg font-semibold">{company?.name || 'Acceso seguro'}</p><p className="text-sm text-slate-400">Portal público de documentos</p></div></header>{children}<p className="mt-6 text-center text-xs text-slate-500">Este enlace es temporal y está protegido por NovaHub.</p></div></main>; }

function OtpForm({ token, mode, maskedTarget, onVerified }: { token: string; mode: 'document' | 'portal'; maskedTarget?: string | null; onVerified: (session: string) => void }) { const [challengeId, setChallengeId] = useState<string | null>(null); const [code, setCode] = useState(''); const [message, setMessage] = useState(''); const requestCode = async () => { try { const result = await publicRequest(`/public-access/${mode}/${token}/otp`, { method: 'POST' }); setChallengeId(result.challengeId); setMessage(result.devCode ? `Código de desarrollo: ${result.devCode}` : `Enviamos un código a ${result.maskedTarget}.`); } catch (error: any) { setMessage(error.message); } }; const verify = async () => { try { const result = await publicRequest(`/public-access/${mode}/${token}/verify`, { method: 'POST', body: JSON.stringify({ challengeId, code }) }); onVerified(result.sessionToken); } catch (error: any) { setMessage(error.message); } }; return <div className="mx-auto max-w-md rounded-2xl border border-slate-800 bg-slate-900 p-7"><LockKeyhole className="mb-4 size-8 text-emerald-400" /><h1 className="text-xl font-semibold">Verifica tu acceso</h1><p className="mt-2 text-sm text-slate-400">Por seguridad necesitamos confirmar tu identidad{maskedTarget ? ` en ${maskedTarget}` : ''}.</p>{message && <p className="mt-4 rounded-lg bg-slate-800 p-3 text-sm text-emerald-300">{message}</p>}{!challengeId ? <button onClick={requestCode} className="mt-6 w-full rounded-xl bg-emerald-500 px-4 py-3 font-semibold text-slate-950">Enviar código</button> : <div className="mt-6 space-y-3"><input value={code} onChange={e => setCode(e.target.value)} inputMode="numeric" maxLength={6} placeholder="Código de 6 dígitos" className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3 outline-none focus:border-emerald-400" /><button onClick={verify} className="w-full rounded-xl bg-emerald-500 px-4 py-3 font-semibold text-slate-950">Continuar</button></div>}</div>; }

function DocumentView({ data, permissions }: { data: any; permissions: any }) { const { company, document, customer, items } = data; return <AccessShell company={company}><section className="rounded-2xl border border-slate-800 bg-white p-6 text-slate-900 shadow-2xl print:shadow-none"><div className="flex justify-between border-b border-slate-200 pb-5"><div><h1 className="text-2xl font-bold text-emerald-600">{company.name}</h1><p className="text-sm text-slate-500">Documento emitido por la empresa</p></div><div className="text-right"><h2 className="text-xl font-bold uppercase">{document.number}</h2><p className="text-sm text-slate-500">{document.date ? new Date(document.date).toLocaleDateString() : ''}</p></div></div><div className="grid gap-4 py-5 sm:grid-cols-2"><div><p className="text-xs font-bold uppercase text-emerald-600">Cliente</p><p className="font-semibold">{customer.name}</p><p className="text-sm text-slate-500">{customer.email || customer.phone || ''}</p></div><div className="text-left sm:text-right"><p className="text-xs font-bold uppercase text-slate-500">Estado</p><p className="font-semibold">{document.status || 'Emitido'}</p></div></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="bg-emerald-500 text-white"><th className="p-3 text-left">Descripción</th><th className="p-3 text-right">Cantidad</th><th className="p-3 text-right">Precio</th><th className="p-3 text-right">Total</th></tr></thead><tbody>{items.length ? items.map((item: any, index: number) => <tr key={index} className="border-b border-slate-200"><td className="p-3">{item.description}</td><td className="p-3 text-right">{item.quantity}</td><td className="p-3 text-right">{item.unitPrice}</td><td className="p-3 text-right">{item.total}</td></tr>) : <tr><td colSpan={4} className="p-5 text-center text-slate-500">Sin líneas de detalle</td></tr>}</tbody></table></div><div className="mt-6 ml-auto max-w-xs space-y-2 text-right"><p>Subtotal: {document.subtotal ?? '—'}</p><p>Impuestos: {document.taxAmount ?? '—'}</p><p className="border-t border-slate-200 pt-2 text-lg font-bold text-emerald-600">Total: {document.total ?? '—'} {document.currency}</p></div><div className="mt-8 flex gap-3 print:hidden">{permissions.print && <button onClick={() => window.print()} className="inline-flex items-center gap-2 rounded-xl border border-slate-300 px-4 py-2 font-semibold"><Printer className="size-4" /> Imprimir</button>}{permissions.download && <button onClick={() => window.print()} className="rounded-xl bg-emerald-600 px-4 py-2 font-semibold text-white">Descargar PDF</button>}</div></section></AccessShell>; }

function PortalView({ data }: { data: any }) { return <AccessShell company={data.company}><div className="grid gap-5 md:grid-cols-3"><div className="md:col-span-3 rounded-2xl border border-slate-800 bg-slate-900 p-6"><p className="text-sm text-slate-400">Cliente</p><h1 className="text-2xl font-semibold">{data.customer.name}</h1><p className="text-sm text-slate-400">{data.customer.email || data.customer.phone || ''}</p></div>{[['Facturado', data.summary.totalInvoiced], ['Pagado', data.summary.totalPaid], ['Saldo', data.summary.balance]].map(([label, value]) => <div key={String(label)} className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><p className="text-sm text-slate-400">{label}</p><p className="mt-2 text-2xl font-bold text-emerald-400">{Number(value).toLocaleString()}</p></div>)}<div className="md:col-span-3 rounded-2xl border border-slate-800 bg-slate-900 p-6"><div className="mb-4 flex items-center gap-2"><WalletCards className="size-5 text-emerald-400" /><h2 className="text-lg font-semibold">Documentos</h2></div><div className="space-y-2">{data.documents.map((doc: any) => <div key={`${doc.type}-${doc.document.id}`} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-slate-800 p-4"><div><p className="font-semibold">{doc.document.number}</p><p className="text-sm text-slate-400">{doc.type} · {doc.document.status || 'Emitido'}</p></div><p className="font-semibold text-emerald-400">{doc.document.total ?? '—'} {doc.document.currency || ''}</p></div>)}</div></div></div></AccessShell>; }

export function PublicAccessPage({ mode }: { mode: 'document' | 'portal' }) { const location = useLocation(); const token = location.pathname.split('/').filter(Boolean).pop() || ''; const [data, setData] = useState<any>(null); const [gate, setGate] = useState<any>(mode === 'portal' ? { requiresVerification: true } : null); const [error, setError] = useState(''); useEffect(() => { if (mode === 'document') publicRequest(`/public-access/document/${token}`).then(result => { if (result?.requiresVerification) setGate(result); else setData(result); }).catch((e: any) => setError(e.message)); }, [mode, token]); if (error) return <AccessShell><div className="rounded-2xl border border-rose-500/30 bg-slate-900 p-8 text-center"><FileText className="mx-auto mb-3 size-9 text-rose-400" /><h1 className="text-xl font-semibold">Enlace no disponible</h1><p className="mt-2 text-slate-400">{error}</p></div></AccessShell>; if (gate) return <AccessShell><OtpForm token={token} mode={mode} maskedTarget={gate.maskedTarget} onVerified={async session => { if (mode === 'document') { const result = await publicRequest(`/public-access/document/${token}`, { headers: { 'x-public-session': session } }); setData(result); setGate(null); } else { const result = await publicRequest('/public-access/portal/session', { headers: { 'x-public-session': session } }); setData(result); setGate(null); } }} /></AccessShell>; if (!data) return <AccessShell><div className="p-12 text-center text-slate-400">Cargando documento…</div></AccessShell>; return mode === 'document' ? <DocumentView data={data.data} permissions={data.permissions} /> : <PortalView data={data} />; }
